// Asset component registry — maps asset id → React component.
// Add new asset components here when created.
import Fireplace from './Fireplace'

export const ASSET_COMPONENTS = {
  fireplace: Fireplace,
  // candle: Candle,
  // rain-window: RainWindow,
}
